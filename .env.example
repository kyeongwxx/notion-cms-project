# ============================================================================
# Notion API 환경 변수
# ============================================================================
#
# 이 파일은 환경 변수 템플릿입니다.
# 실제 값은 .env.local 파일에 입력해주세요.
#
# 설정 방법:
# 1. 이 파일을 .env.local로 복사: cp .env.example .env.local
# 2. 아래 값들을 실제 값으로 교체
# 3. .env.local 파일은 절대 Git에 커밋하지 마세요!
# ============================================================================

# Notion Integration Token
# 발급 방법:
# 1. https://www.notion.so/my-integrations 접속
# 2. "New integration" 버튼 클릭
# 3. Integration 이름 입력 (예: "내 블로그")
# 4. "Submit" 버튼 클릭
# 5. "Internal Integration Token" 복사
# 형식: ntn_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
NOTION_API_KEY=your_notion_api_key_here

# Notion Database ID
# 발급 방법:
# 1. Notion에서 블로그 글이 저장된 데이터베이스 페이지 열기
# 2. 페이지 URL 확인: https://www.notion.so/{워크스페이스}/{DATABASE_ID}?v={VIEW_ID}
# 3. DATABASE_ID 부분을 복사 (32자리 영숫자, 하이픈 제거)
# 4. 데이터베이스를 Integration과 공유:
#    - 데이터베이스 우측 상단 "..." 메뉴 클릭
#    - "Add connections" 클릭
#    - 생성한 Integration 선택
# 형식: 32자리 영숫자 (하이픈 없음)
NOTION_DATABASE_ID=your_notion_database_id_here

# ============================================================================
# 선택 사항 (향후 확장)
# ============================================================================

# Notion 캐시 설정 (초 단위)
# NOTION_CACHE_TTL=60

# Notion API Rate Limit 설정 (요청/초)
# NOTION_RATE_LIMIT=3
